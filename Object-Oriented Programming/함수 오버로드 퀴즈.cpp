#include <iostream>
using namespace std;

/*
int big(int a, int b) {
	if (a < 100 && b < 100) {
		if (a < b) return b;
		else return a;
	}
	else {
		return 100;
	}
}

int big(int a, int b, int c) {
	int m = (a > b) ? a : b;
	int x = (m > c) ? m : c;

	return x;
}

int main() {
	int x = big(3, 5);
	int y = big(300, 60);
	int z = big(30, 60, 50);
	cout << x << y << z << endl;
}*/